{{ define "widget-teachers" }}
<div class="row">
	<div class="col s12">
		<div class="card">
			<div class="card-content">
				<p class="header">Teachers</p>
				<div class="collection">
				{{ range .Listings.Teachers.Teachers}}
					<a href="#!" class="collection-item">{{ .Name }}</a>
				{{ end }}
				</div>
			</div>
		</div>
	</div>
</div>
{{ end }}