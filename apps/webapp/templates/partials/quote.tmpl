{{ define "quote" }}
	<div class="row">
		<div class="col s12">
			<div class="card blue-grey darken-1">
				<div class="card-content white-text">
					<h5>"{{ .Text }}"</h5>
					<h5 class="right">--{{ .Teacher }}</h5>
				</div>
			</div>
		</div>
	</div>
{{ end }}