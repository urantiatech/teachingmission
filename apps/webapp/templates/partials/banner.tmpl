{{ define "banner" }}
	<div class="row">
	    <div class="col s12">
	        <div class="card">
	            <div class="card-image">
	                <img src="{{ .Url }}">
					<span class="card-title">{{ .Text }}</span>
	            </div>
	        </div>
	    </div>
	</div>
{{ end }}