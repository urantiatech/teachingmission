{{ define "article" }}
	<div class="row">
		<div class="col s12">
			<div class="card blue-grey darken-1">
				<div class="card-content white-text">
					<span class="card-title">{{ .Title }}</span>
					<p>"{{ .Text }}"</p>
				</div>
			</div>
		</div>
	</div>
{{ end }}